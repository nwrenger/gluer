openapi: 3.0.0
info:
  title: test
  version: 0.1.0
paths:
  /:
    get:
      operationId: root_get
      responses:
        '200':
          description: 200 response
          content:
            application/json:
              schema:
                $ref: '& ''static str'
    post:
      operationId: root_post
      responses:
        '200':
          description: 200 response
          content:
            application/json:
              schema:
                $ref: '& ''static str'
